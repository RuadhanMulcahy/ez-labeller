import React, { useRef } from "react";
// import ICoordinate from "../@types/ICoordinate";
// import ILabel from "../@types/ILabel";
// import IOffsets from "../@types/IOffsets";

// const Label = (label: ILabel) => {
//   const labelCanvasRef = useRef<HTMLCanvasElement>(null);
//   const canvas = labelCanvasRef.current;

//   if (canvas) {
//     canvas.width = label.width;
//     canvas.height = label.height;
//   }

//   //   function drawBox(ctx: CanvasRenderingContext2D) {
//   //     const canvas = labelCanvasRef.current;

//   //     if (canvas && label.boxTopLeftCorner && ctx) {
//   //       ctx.beginPath();
//   //       ctx.strokeStyle = "#ff0000";
//   //       ctx.setLineDash([0, 0]);
//   //       ctx.strokeRect(
//   //         label.boxTopLeftCorner.x,
//   //         label.boxTopLeftCorner.y,
//   //         label.mousePos.x - label.boxTopLeftCorner.x - label.offsets.left,
//   //         label.mousePos.y - label.boxTopLeftCorner.y - label.offsets.top
//   //       );
//   //     }
//   //   }

//   return <canvas ref={labelCanvasRef}></canvas>;
// };

// ba

// export default Label;
